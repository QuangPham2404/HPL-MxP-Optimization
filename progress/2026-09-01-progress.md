# Progress — 2026-09-01, multinode baseline session

## Current step

Setting up HPL-MxP multi-node baseline runs (2x1 … 3x8).

**Status: cross-node MPI communication now WORKS (Approach 1).** The remaining
work is to re-wire the baseline launch script to the new model and re-run the
sweep. See `multi-node-test/README.md` (session hand-off) and
`MANUAL_INSPECTION_ERROR.md` case `mn-2026-09-01-mpi-init-hang`.

## Completed work

- Read the full workflow pack, `APPLICATION.md`, latest progress, and Git state.
- Inspected `multi-node-test/README.md` + `GAAS_MULTINODE_SETUP.md`; confirmed the
  validated pbsdsh launch bridge and its caveats.
- Inspected the container's `hpl-mxp.sh` (per-rank wrapper: reads
  `OMPI_COMM_WORLD_LOCAL_RANK`, maps `--gpu-affinity` -> `CUDA_VISIBLE_DEVICES`).
- Confirmed hardware: H200 SXM 141GB (8/node), host NVHPC 26.3 HPC-X 2.25.1
  OpenMPI 4.1.9a1; container bundles its own HPC-X OpenMPI 4.1.9a1 at `/opt/hpcx`.
- Wrote and committed:
  - `run_hplmxp_multinode_baseline.pbs` (parametrized N×G baseline).
  - `run_hplmxp_baseline_sweep_seq.sh` (sequential 8-config driver).
  - Diagnostic probes: `probe_multinode_mpi.pbs`, `probe_multinode_envfix.pbs`,
    `probe_approach1_recon.pbs`, `probe_approach1_mpi.pbs`.
  - `rsh_pbsdsh_container.sh` (Approach-1 bridge).

## Baseline parameters chosen (per user)

- `--n 120000 --nb 1024 --nporder row`, fixed for every config; grid
  `nprow × npcol = ranks` (square-ish, `npcol >= nprow`). 8 ranks => 2x4 exactly.

## Root cause (confirmed)

Host `mpirun` (host HPC-X) drove a container MPI app (container HPC-X at
`/opt/hpcx`). Same OpenMPI version (4.1.9a1), different build → the container
ranks' ORTE/PMIx runtime could not complete the cross-node session, so `MPI_Init`
hung. Transport (InfiniBand) was fine. Not an env-pollution issue (ldd confirmed
the app links its own `libmpi.so.40` via RPATH).

## The fix (Approach 1) — resolved

Run the **container's own `mpirun`** and launch the remote `orted` **inside the
container** (consistent MPI end-to-end). Three fixes, all in
`rsh_pbsdsh_container.sh`:

1. bind `/opt/pbs` + `/var/spool/pbs` into the container (pbsdsh works in-container).
2. single-quote the reconstructed `orted` command (it contains `"` and `(`).
3. strip `--daemonize` (otherwise the ephemeral `apptainer exec` unmounts the SIF
   and the backgrounded orted hits a `Bus error`).

**Verification (PASSED):** `probe_approach1_mpi.pbs` (job `55779.gaas`) — all 4
ranks `after MPI_Init` and `allreduce sum=6 expect=6`.

## Next action (hand-off to next agent)

1. Update `run_hplmxp_multinode_baseline.pbs` to the Approach-1 launch
   (container `mpirun` + `rsh_pbsdsh_container.sh`); see README recipe.
2. Validate the 2×2 HPL baseline (expect finite residual + GFLOPS).
3. Run the full sequential sweep 2×1 … 3×8.
4. Update `GAAS_MULTINODE_SETUP.md` to mark host-`mpirun` model superseded.